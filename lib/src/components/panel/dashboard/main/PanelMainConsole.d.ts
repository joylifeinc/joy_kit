/// <reference types="react" />
import * as React from 'react';
export interface Props {
    children?: React.ReactNode;
    showPanel: boolean;
}
export declare const PanelMainConsole: ({children, showPanel}: Props) => JSX.Element;
